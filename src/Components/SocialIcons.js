import { SocialIcon } from 'react-social-icons';
const SocialIcons = ({ url }) => {
  return (<SocialIcon url={url} fgColor='#fff' bgColor='#000' style={{ height: '30px', width: '30px' }} />);
}

export default SocialIcons;
